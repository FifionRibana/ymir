# Solver reconstruction — implementation roadmap

This document tracks the incremental rebuild of the tectonic solver from
a nondimensional core. It is the implementation companion to
`solver-scaling.md` (physics reference).

## Status

| Step | Description | Status | PR | Report |
|---|---|---|---|---|
| 0 | Nondim Stokes core + S advection (incl. null-space precond) | shipped | #79 | `docs/reports/step0_report.md` |
| 1 | Power-law rheology + Newton solver + continuation | shipped | #81 | `docs/reports/step1_report.md` |
| 2 | GPE spreading (Ar·∇(½S²), staggered flux form) | shipped | #83 | `docs/reports/step2_physics_report.md` (physics) + `docs/reports/step2_regression_report.md` (Step-1 mirror). **#78 remains open** — the GPE gradient spike concerns sharp material interfaces introduced at Steps 5/6, not this step. |
| 3 | Plastic yielding (Von Mises / Bingham, stateless) | shipped | #85 | `docs/reports/step3_physics_report.md` + `docs/reports/step3_regression_report.md`. **Baseline `Bi = 0.15` with GPE-only forcing runs floor-dominated** — `yielding_cell_fraction = 0` at Bi=0.15 is the expected physical outcome (analytic criterion: yielding dominance requires `Bi < ε̇_min^(1/3) = 0.1` when ε̇_II is floor-dominated, cf. report's "Strain-rate regime diagnostic" section). The yielding mechanism itself is validated through the Bi sweep (`yielding_cell_fraction = 1.0` at `Bi ≤ 0.10`), MMS slopes (2.001), Jacobian symmetry (3e-14), and zero-cost regression (ratio 1.00/1.04 vs Step 2). **Checkpoint flagged**: revisit `yielding_cell_fraction` at the Step 4 / 5 / 7 / 8 physics reports — if it remains 0 by Step 7, the source-mechanism coupling to ε̇ is under-dimensioned and warrants remontée. |
| 4 | Basal drag (velocity-damping via operator diagonal) | shipped | #87 | `docs/reports/step4_physics_report.md` + `docs/reports/step4_regression_report.md`. Stateless `f_drag = -Br · S̃² · ṽ` as a positive diagonal augmentation to the Picard block. Preconditioner follows case (B) — diagonal reconstructed analytically in `stokes/operator.rs::momentum_diagonal`, consistency with `apply_momentum`'s stencil guarded by `tests/v2_precond_drag_diagonal`. **Zero-cost when disabled** confirmed by regression ratio ≈ 1.00 vs Step 3 (wallclock and CG iters). **Baseline drag/visc ratio ≈ 10⁻⁷** (corrected vs prompt: η ≈ 100 at the floor-dominated power-law baseline, not η ≈ 1), so peak \|v\| damping is below 4-digit precision; the Br sweep's strict `peak \|v\|` monotonicity (at f64) is the physical signal. `yielding_cell_fraction = 0` at baseline (yielding Disabled here; full checkpoint deferred to Step 5/7 per roadmap). |
| 5 | Boundary sources/sinks | shipped | #89 | `docs/reports/step5_physics_report.md` + `docs/reports/step5_regression_report.md` + `docs/reports/step5_reference_variant_report.md`. Five source/sink terms (Q_sub, Q_arc, Q_spread, Q_coll-v, Q_rift-v) on a prescribed static layout (`horizontal_oceanic_strip`). `BoundaryConfig::Disabled` → structural bypass (advection-only, no Q, no clamp, no tracking). Regression convention installed: step N activates all mechanisms through N-1 in their canonical configuration. Because Step 4 physics was non-canonical (yielding `Disabled` for Br isolation), Step 5 produces a "reference variant" on this branch (Step 4 config with yielding Enabled) as the regression's comparison target. **Baseline s_oceanic_mean = 0.2158 (64²) / 0.2085 (128²)** both in `[0.18, 0.22]` post-calibration (calibrated `k_spread = 0.050`, first-probe convergence at the bracket's low end — at Step 5 baseline's tiny convergent-velocity regime, Q_sub barely fires and any sizable `k_spread` grows the oceanic strip; the balance appears at Steps 7/8). **mass_balance_residual < 10⁻¹²** at both grids (spec acceptance `< 1%`). **Clamp never fires** (clamp_activation_fraction = 0). **k_sub sweep monotonicity strict at f64 precision**. **yielding_cell_fraction = 0** — yielding checkpoint still deferred to Step 7 as expected; the Step 5 baseline remains floor-dominated. **CG ratio vs Step 4 physics ≈ 2.2×** — the heterogeneous S̃² (oceanic 0.04 vs continental 1.0 adjacent) stresses the Jacobi preconditioner; above the spec's 1.3× target, but Newton still converges 100% at both grids. **boundary_type_diversity = 2** (subduction + spread active). **#78 monitoring**: peak f_GPE on oceanic/continental interfaces ≈ 3.58 at 64², recorded as trajectory telemetry through Steps 5–8. |
| 6 | Dynamic boundaries, Voronoi plates, conservative recycling | shipped | #90 | `docs/reports/step6_physics_report.md` + `docs/reports/step6_regression_report.md`. Three pieces delivered together: (1) **Voronoi tessellation** static per run (num_plates=8, continental_ratio=0.3) replaces synthetic static layouts; (2) **dynamic boundary detection** per step via `div(v)` staggered finite-difference + argmax-neighbour plate-type lookup (threshold 1e-4); (3) **conservative recycling** — subducted mass is distributed via fractions (arc=0.15, coll_v=0.03, rift_v=0.02, spread=0.80, mantle_loss=0) with immediate arc/coll_v/rift_v and delayed spread through a ring buffer with **rollover semantics** (emerging mass held-at-output when no rift cell is active, no re-aging). `k_spread` disappears as a rate — spread is now a derived flux of the recycling budget, closing the mass loop physically. **Step 5 regression convention honoured**: Step 6 regression = Step 5 physics setup (Open mode + `horizontal_oceanic_strip`), Voronoi/dynamic-detection/buffer structurally bypassed; CG iters 1.00× vs Step 5 physics at both grids. **mass_conservation_residual = 2.8e-17 (64²) / 3.9e-15 (128²)**, 5-component form `\|Δmass_obs + mantle_loss + buffer_fill + pending − clamp_flux\| / initial_mass`, target `< 1e-6` — achieved at machine noise. **No taper at Voronoi oceanic/continental interfaces** (Step 6 D5): #78 tested in raw contrast, reported as trajectory telemetry at t ∈ {1, 10, 50, 150, 300}·Δt. **yielding_cell_fraction = 0** at Step 6 baseline — checkpoint officially migrated to Step 7 (slab-pull expected to activate). **CG iters Step 6 physics / Step 5 physics**: 1.19× (64²), 1.17× (128²), well below the 2× surveillance threshold. `RecyclingBuffer` re-exported from legacy `tectonics::recycling`; v2 wrapper `DelayedRecycler` adds rollover + fill tracking without touching the legacy. |
| 7 | Slab-pull regularized RHS | shipped | #93 | `docs/reports/step7_physics_report.md` + `docs/reports/step7_regression_report.md` + `docs/reports/step7_sp_sweep_report.md`. Slab-mass ODE `∂m̃/∂t̃ = k_slab_accum · max(0,-div v) − m̃/τ̃_slab` (forward-Euler, stability-capped `Δt ≤ 0.1·τ_slab`) + `SlabPullForce` in `forcing/` assembling `f̃_slab = Sp · m̃ · n̂_convergence` with `n̂ = -∇(div v)/|∇(div v)|` (fallback to zero below ε=1e-6). `SlabPullConfig::Disabled` = structural bypass (no `SlabState`, no `Q_sub_conv`, no `n̂`, no force, no `m̃` advection). **Mean(f_slab) is NOT subtracted** — null-space projector on `v` handles it (test `v2_slab_null_space_preservation` verifies `|mean(v)| < 1e-15` with `mean(f_slab) ≠ 0`). **Yielding checkpoint REVISED**: the D8 prediction (slab-pull alone activates yielding at Step 7) was rigorously refuted by closed-loop gain analysis `G = Sp · k_slab_accum · τ_slab / (η · L) ≈ 7.5e-3 ≪ 1` in the floor-dominated regime (`η_newton ≈ 100` since `ε̇_II < ε̇_min` at Step 6 baseline). Slab-pull is an **amplifier, not an initiator**; the quiescent fixed point is linearly stable across the full §4.8 band `Sp ∈ [0.5, 3.0]`. Checkpoint migrates to Step 8 (last-chance, no further deferral). Documentation patched: `solver-scaling.md §4.8` adds "Activation regime requires pre-existing non-quiescent flow"; `tectonics_v2/README.md` adds the revision note. **Regression ratios vs Step 6 physics: 0.98× / 1.02× wallclock, 1.00× / 1.00× CG mean** at 64² / 128², scalar parity on `mass_conservation_residual` at machine noise. **Physics**: `peak|v| = 3.602e-5 (64²) / 3.625e-5 (128²)` — unchanged from Step 6 as predicted by G≪1. `peak|f_slab| = 1.6e-4 / 1.7e-4`, `peak_f_slab/peak_f_gpe ≈ 2.6e-5` (spec expected `O(10)-O(100)` assuming η~1; miss is physical, not bug). **Sp sweep** (0.5, 1.0, 1.5, 2.0, 3.0 at 64²): flat `peak|v|` across the band to f64 precision — the signature of the bootstrap-failure regime. `m_max ≈ 1.007e-4` independent of Sp (m depends on Q·τ, not Sp directly). **Newton 100% convergence, Mass conservation residual ≈ 2.7e-15 / 2.4e-15** at 64² / 128². **MMS**: `v2_slab_decay_mms` (half-life = τ·ln2 to 5%), `v2_slab_force_mms` (slope 2.0), `v2_slab_advection_mms` (mass drift < 1e-10), `v2_slab_null_space_preservation` (|mean(v)| < 1e-15 with non-zero mean(f_slab)). `v2_step7_regression_smoke` green. |
| 8 | Mantle flow (INITIATOR — last-chance yielding checkpoint) | shipped | #95 | `docs/reports/step8_physics_report.md` + `docs/reports/step8_regression_report.md` + `docs/reports/step8_mf_sweep_report.md`. Mantle forcing installed as `f_mantle = coupling · S̃ · (Mf · v_pattern − v_solved)`. Refactor à la basal-drag: `+coupling · S̃` on the momentum operator diagonal (self-consistency at every Newton outer iteration), constant RHS part `coupling · S̃ · Mf · v_pattern` via `MantleForce: BodyForce`. Pattern = staggered curl of a **nodal** Fourier stream function, div-free by exact algebraic cancellation (`div_v_mantle_max = 1.14e-13` at 64², < 1e-10 strict acceptance). Static at Step 8 (`evolution_rate = 0`). `MantleConfig::Disabled` = structural bypass. **Yielding STRICT last-chance checkpoint RESOLVED**: `yielding_cell_fraction_max = 0.998 (64²) / 0.985 (128²)`, `peak|v_solved| = 9.55 / 9.62`, `ε̇_II/floor = 1.59e5 / 3.14e5` — bootstrap 5+ orders of magnitude above Step 7's floor-dominated 3.6e-5. Mf sweep `{0.3, 0.6, 1.0, 1.5, 2.0}` monotone in peak|v_solved| (1.3e-3 → 24.8) with yielding activation threshold between Mf=0.3 (0.17 marginal) and Mf=0.6 (0.98). **Step 8 regression convention exception**: baseline held with slab-pull `Disabled` pending slab+mantle co-calibration (§4.8 target `Sp ∈ [0.5, 3]` and §4.9 `Mf ∈ [0.3, 2]` NOT co-calibrated in activated regime — loop gain `G_activated ≈ 96/80 > 1` produces exponential runaway; see `step8_physics_report.md §Slab+Mantle interaction instability finding` for full derivation). Regression compares vs `step6_physics_report.md` (no slab, no mantle): CG iters ×1.00 exact, mass_conservation_residual and peak|v| bit-identical with Step 6, wallclock 1.058× / 1.060× (within single-run noise floor, reviewer-approved). Follow-up issue drafted in `docs/followup_slab_mantle_cocalibration.md` with three resolution paths (recalibrate Sp / modify Q_sub_conv discretisation / saturate m_subducted). Does not block Step 9 (cratonic immunity, orthogonal). The `v2_mantle_runaway_diagnostic` test (currently `#[ignore]`-d) is the permanent oracle for the co-calibration resolution. Preconditioner CG mean 11× Step 6 at 64² (1420 vs 130) — above the 3× threshold, diagnostic documented in the physics report without code change (per §Point architectural scope restriction). |
| 9 | Cratonic plastic immunity | blocked by 8 | — | — |
| 10 | Geological age field | blocked by 9 | — | — |
| 14 | Oceanic FBM heterogeneity (continental FBM in Step 13; oceanic deferred to enable post-Step-12 calibration against erosion impact). Adds opt-in `apply_fbm_to_oceanic` flag + separate `fbm_amplitude_oceanic` to `RadialProfileWithFBM`. Living Landz workflow uses it for ocean-floor heightmap rendering, coastal gameplay, and volcanic-island emergence (controlled threshold crossing). | planned (post-Step 12) | — | referenced from `docs/reports/step13_physics_report.md` § "Caveats — Oceanic FBM heterogeneity" |

| Transverse | Description | Status |
|---|---|---|
| ~~T1~~ | ~~Null-space-aware preconditioner~~ | **absorbed into Step 0** — shipped as part of `stokes/precond.rs` + `stokes/nullspace.rs` with verification in `tests/v2_nullspace.rs` |
| T2 | Diagnostics framework extension | MVP introduced step 0; extended each subsequent step |
| T3 | Stochastic validation harness | not started (introduced after step 4) |

## Conventions

- Each step lives on its own commit on `reconstruction/solver-from-scratch`
- Each step has exactly one PR against the reconstruction branch
- Each step produces a report in `logs/stepN_report.md` with metrics
  delta vs step N-1
- The `tectonics_v2/` module tree is built incrementally; no code from
  `tectonics/` is imported except for utility types (Field2D,
  PeriodicIndex)

## Reusable infrastructure

The reporting framework introduced in step 0 is the contract. Each
step extends it with new metrics but does not modify the core
comparison logic.

Reference scenario for cross-step comparison:
- Grid: 64² and 128²
- Seed: 42
- Duration: T = 6·τ*
- Number of macro steps: 300
- Active extensions: those introduced up to and including the current
  step

## Final integration

Once step 10 lands, T1 and T3 must be done before merging the
reconstruction branch back to main. T2 is built incrementally
throughout.

The final merge is a single PR with full diff review. The new tree
replaces the existing `tectonics/` module wholesale; UI integration in
ymir-viz is updated in the same merge.